# Visual pass log — DATUM (MS-SEC-CONT01)

Operator demo: `http://localhost:3004/demo/cleanroom-datum`  
Tool: browser-harness

## PASS 1

- Rest: letter + three rules + Send the note. No how-to. No site chrome. Overflow-x false.
- Focus: label inks, focus hairline `scaleX(1)`.
- Send: Sent.
- Defects: copy/form sat on the bottom third (`align-items: end`). Site `:focus-visible` ring boxed Name and Note.

## PASS 2

- Body centers on desktop (`h2` ~369px in 900).
- Focus: `box-shadow: none`. Cursor on the Name rule. No kit box.
- Mobile 390: stack, no overflow-x, fields stay lines.

No blocking visual defects. GOLDEN for plant.

Open for operator taste: two-column split, Troy as the restage slot, 2-row note.
