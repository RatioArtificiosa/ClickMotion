# Elements area (later)

**Status:** Stub. Out of scope.  
**Do not** build a Three.js / WebGL / canvas gallery yet.

This aisle will later hold **elements**: Three.js scenes, shaders, particle fields, cursor-reactive canvases, small 3D objects that are not full heroes or mid-page sections.

Related live craft (do not fork here): Helix helix-orbit, Zero Energy 3D gallery, Folio glass, Phobia cursor.

Until the operator opens this scope:

- WebGL in a **section** is allowed only when the kind needs it and PRODUCT_LAW tags are honest (`3d-threejs`, `webgl`)
- Do not default Features/Pricing to a Three.js toy
- Do not create an `/elements` route

When we start: write `LAW.md` here first. Performance budget and one-idea-per-element will be mandatory.
