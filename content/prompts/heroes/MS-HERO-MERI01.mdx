---
id: "MS-HERO-MERI01"
title: "MERIDIAN - Scroll Narrative Private Residences Hero"
slug: "meridian-scroll-narrative-private-residences-hero"
description: "For private residences and ultra-luxury estates. Guests advance a cinematic shoreline story as they scroll - quiet authority that builds desire before the first tour."
version: "1.0.0"
created: "2026-08-08"
updated: "2026-08-08"
author: "MS Team"
status: "published"
type: "hero"
category: "real-estate"
subcategory: "luxury-real-estate"
styleTags: ["luxury", "dark-cinematic", "editorial"]
technicalTags: ["video-background", "scroll-trigger"]
motionIntensity: "aggressive"
difficulty: "advanced"
priceTier: "pro"
aiToolsRating:
  cursor: 5
  lovable: 3
  bolt: 3
  claude: 5
  grok-build: 5
frameworksSupported: ["react"]
previewVideo: "/assets/videos/meridian-scroll-preview-v1.mp4"
thumbnail: "/thumbnails/MS-HERO-MERI01.webp"
liveDemo: "/demo/scroll-narrative"
videoBackgrounds:
  - file: "/assets/videos/sequence-01.mp4"
    format: "mp4"
    duration: "12s"
    loop: false
    sizeMb: 19.5
    poster: "/assets/posters/sequence-01.webp"
dependencies:
  - { name: "gsap", version: "^3.12.0", required: true }
  - { name: "tailwindcss", version: "^3.4.0", required: true }
estimatedTokens: 6800
useCases: ["luxury-real-estate-homepage", "private-residences-hero", "scroll-narrative-landing"]
compatibleWith: []
positionInPage: "top"
---
## Design System

**Aesthetic Direction:** Ultra-luxury private residences - **MERIDIAN**. Dark cinematic frame (`#0c0a08`) over a golden-hour Atlantic estate film. Cream type (`#f7f1e8`), single gold accent (`#c9a66b`). Editorial serif display (Cormorant Garamond or Playfair Display), Inter for UI. Quiet, expensive, unhurried - Aman / Four Seasons Residences craft × Apple product-film pacing. **Never** SaaS purple, aurora mesh, pill glass nav, border beams, shiny gradient text, or emoji.

**Famous-UI craft direction (not a clone):** Four Seasons Private Residences editorial calm + linear scroll storytelling (Cartier / Shopify Editions class restraint). One signature system: scroll owns the film.

**Typography:**
- Display / H1: Cormorant Garamond (or Playfair Display), weight 500, tracking `-0.02em`, line-height `0.96`, size `clamp(2.75rem, 7.2vw, 5.85rem)`. Two stacked lines via block spans.
- Body: Inter 300-400, 15-17px, line-height 1.7, color cream at 78% opacity.
- Eyebrows / nav: Inter 10-11px, uppercase, tracking `0.18em-0.28em`, gold for active eyebrows.

**Color tokens:**
| Token | Hex |
|-------|-----|
| Canvas | `#0c0a08` |
| Cream | `#f7f1e8` |
| Gold | `#c9a66b` |
| Soft gold | `#e8d5b0`, `#f0d9a8` |
| Linear scrim | `rgba(8,6,4,0.55)` → mid open → bottom `0.72` |
| Radial vignette | soft ellipse, center clear |

**Spacing:** 8px grid. Content max-width `1400px`. **Safe horizontal inset minimum `px-8` / 2rem** (prefer `md:px-14` `lg:px-16`) so type never clips at frame edges or under gallery `object-cover` crop. Display type: `min-w-0`, `break-words`, clamp capped so lockups stay inside ~84% center. Nav height `4.5rem`. Rectangular CTAs (not pills) - border-radius `0`.

**Anti-slop hard ban:** floating glass pill dock nav, mesh/aurora/warp backgrounds, shiny text, marquee, bento clutter, Inter-only heavy bold H1, more than one gold system.

---

## Layout Structure

```
[Scroll track 420vh desktop; 100vh if reduced-motion]
 [Sticky 100vh stage]
 video cover (absolute inset-0)
 dual scrim (linear + radial)
 header: MERIDIAN | Private Residences · links · Request Access
 1px gold progress under nav (scaleX = scroll progress)
 chapter copy bottom-aligned (grid 12: 8 copy + 4 chapter index 01/02/03)
 scroll cue word + thin gold line (only while progress < 0.04)
[Closing band #request]
 membership headline + 3 stats + mailto CTA
```

**Navbar:** Absolute over film, not sticky site chrome. Wordmark MERIDIAN (display, tracking wide). Desktop links: Residences, Architecture, Locations, Concierge. CTA: outlined gold translucent “Request Access” - rectangular.

**Chapter copy:** Bottom-left of stage. Eyebrow gold → H1 two lines → body. Right column desktop: vertical chapter markers `01 / 02 / 03` with active gold dash.

**Do not** print instructional copy like “Scroll to move through the residence” or “never autoplayed.” The word **Scroll** (cue) + progress line + chapter markers are enough.

**Closing band:** Dark canvas, gold “Membership” eyebrow, display headline “Reserved for those / who already have everything.”, supporting copy, stats `12 Residences · 4 Coastlines · 100% Owners only`, outlined gold “Begin a conversation” mailto.

---

## Content Slots

| Slot | Default | Notes |
|------|---------|-------|
| brand | MERIDIAN | Display wordmark |
| chapter_01_eyebrow | Private Atlantic · By Appointment | Gold uppercase |
| chapter_01_title | The coastline / belongs to few. | Two lines |
| chapter_01_body | A rare line of oceanfront residences where architecture, light, and silence are curated as carefully as capital. | |
| chapter_02_eyebrow | Interiors · Bespoke | |
| chapter_02_title | Every ascent / is intentional. | Two lines |
| chapter_02_body | Hand-finished stone. Warm mahogany. Soft architecture that leads the eye - and the guest - toward the horizon. | |
| chapter_03_eyebrow | The Arrival | |
| chapter_03_title | Where the day / ends in gold. | Two lines |
| chapter_03_body | A private path from lawn to shore. Reserved for owners who measure success not in square feet, but in unbroken views. | |
| cta_primary_ch3 | Schedule a private tour | Solid cream on dark |
| cta_secondary_ch3 | View the portfolio | Outline cream |
| nav_cta | Request Access | Gold outline |
| membership_title | Reserved for those who already have everything. | Closing band |

### Chapter ranges (scroll progress 0-1)

| # | Range | Active when |
|---|-------|-------------|
| 01 | 0.00-0.32 | Aerial estates |
| 02 | 0.32-0.62 | Interiors / ascent |
| 03 | 0.62-1.00 | Path to ocean + CTAs |

Only one chapter title visible at a time. On chapter 03 only: show the two CTAs.

---

## Motion Specification

**Signature mode: scroll-as-narrative.** Scroll progress drives `video.currentTime`. Video never autoplays and never loops as wallpaper.

```tsx
// GSAP ScrollTrigger - scrub video timeline
ScrollTrigger.create({
 trigger: trackRef.current, // height ~420vh
 start: "top top",
 end: "bottom bottom",
 scrub: 0.45,
 onUpdate: (self) => {
 const p = self.progress; // 0..1
 const v = videoRef.current;
 if (v?.duration) {
 const t = p * v.duration;
 if (Math.abs(v.currentTime - t) > 0.016) v.currentTime = t;
 }
 // progressBar style transform = `scaleX(${p})` transformOrigin left
 // setActiveChapter from ranges above
 },
});
// video: muted playsInline preload="auto" - no autoPlay, no loop; always pause()
```

| Motion | Spec |
|--------|------|
| Track height | `420vh` (desktop); `100vh` if `prefers-reduced-motion` |
| Stage | Sticky `100vh` |
| Scrub | `0.45` |
| Chapter enter | opacity 0→1, y 18→0, 0.85s, `cubic-bezier(0.22, 1, 0.36, 1)` |
| Progress bar | direct `scaleX(progress)`, origin left |
| Scroll cue | visible only when progress &lt; 0.04; thin pulsing gold line under the word SCROLL |
| Reduced motion | No scrub; poster + chapter 01 still |

No Framer layout animations required. Optional CSS keyframes for chapter crossfade only.

---

## Video / Media Integration

| Field | Value |
|-------|--------|
| File | `/assets/videos/sequence-01.mp4` |
| Poster | `/assets/posters/sequence-01.webp` |
| Attributes | `muted playsInline preload="auto"` - **NO autoplay, NO loop** |
| Object-fit | `cover`, full sticky stage |
| Duration | ~12s @ 24fps |
| Control | `currentTime = progress * duration` |

**Required subject:** Luxury beachfront estates at sunset - continuous journey aerial coastline → warm interiors / staircase → garden path to ocean horizon. Warm gold light, palms, manicured grounds. Private wealth mood.

**Forbidden:** Neon, cyberpunk, cities, aircraft, faces to camera, on-screen text/logos, vertigo cuts.

```txt
Use ONLY the paths above. Never hotlink foreign CDNs. Scroll is the transport.
```

> **AI Video Generation Prompt:** Cinematic 4K continuous single-take feel, ~12 seconds, no audio, 24fps. Sequence as one journey: (1) slow aerial over Mediterranean-revival beachfront mansions at golden hour sunset, Atlantic waves, warm orange sky; (2) dissolve into ascending limestone staircase with dark mahogany banisters, recessed step lights, warm interior glow toward bright ocean windows; (3) emerge onto manicured lawn path between palms and hedges leading to beach gates and open sunset ocean horizon. Ultra luxury real estate atmosphere, natural light only, no people faces, no text, no UI, no logos. Smooth camera, expensive restraint. Seamless emotional arc.

Encode: MP4 H.264, 1920×1080, silent, progressive, `+faststart`. Prefer frequent keyframes (e.g. keyint 12) for scrub seeking.

---

## Responsive Behavior

| Breakpoint | Behavior |
|------------|----------|
| ≥1280px | Full 12-col layout; chapter index right; nav links visible; track 420vh |
| ≥768px | Nav links visible; H1 clamp; CTAs row on ch03 |
| &lt;768px | Nav links hidden; H1 smaller; chapter index under copy or compact; track still tall for scrub; CTAs stack |
| Reduced motion | Track 100vh; no scrub; chapter 01 + poster |

---

## Accessibility

- Honor `prefers-reduced-motion: reduce` - static chapter 01, no tall scrub track
- Semantic `header`, `section`, `h1`, real focusable links for all CTAs
- Cream text on dark scrim meets contrast; never thin gray on bright sky without scrim
- Native document scroll only - do not trap focus or hijack wheel into a custom scroller
- Progress bar is decorative; chapters readable without color alone (numbers 01-03)

---

## Performance Notes

- Single `<video>`; always paused; seek only when `|Δt| > 0.016s`
- Kill ScrollTrigger on unmount
- Poster first paint; `preload="auto"` acceptable for this hero
- Cap re-renders: chapter state changes only when index changes
- One WebGL/canvas forbidden for this SKU - video + DOM only
- Prefer mid-weight encode (~10-20MB) with dense keyframes over 4K 50MB+

---

## AI Tool Instructions

**Cursor / Claude / Grok Build:** One file `MeridianScrollNarrative.tsx` (or `HeroSection.tsx`), `"use client"`, default export. Import `gsap` + `ScrollTrigger`, register plugin. Implement scrub mapping, three chapters, nav progress, membership band. Use Tailwind utilities; load Cormorant Garamond + Inter via `next/font/google` on the page wrapper if Next.js.

**Lovable / Bolt:** Same structure; if GSAP is awkward, implement scroll listener with `requestAnimationFrame` that sets `video.currentTime = progress * duration` - still no autoplay loop. Keep exact copy and colors.

**Do not** install Magic UI / Aceternity / Lightswind. Do not add aurora backgrounds. Do not make the nav a floating pill dock.

---

## Expected Output

1. Single production React + TypeScript + Tailwind component, default export
2. Scroll track ~420vh with sticky 100vh cinematic stage
3. Video scrubbed by scroll (no autoplay wallpaper)
4. Three chapter copy states at specified progress ranges; two-line display titles
5. Gold progress line under nav; chapter markers 01-03
6. Scroll cue only at start (word + line) - no instructional paragraph about autoplay
7. Chapter 03 CTAs + closing membership band
8. `prefers-reduced-motion` path
9. Asset paths exactly as specified
10. Looks Forbes / private-residences grade on 1440px - not an AI kit skin
